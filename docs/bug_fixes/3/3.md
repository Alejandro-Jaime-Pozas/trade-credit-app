frontend
- create credit case page when user clicks submit button, should redirect directly to new credit case, not two-step to get there
  - http://localhost:3000/credit-cases/new
- if user uploads a file in customer/credit case detail page, then moves to another page, then returns to detail page, the Uploading... button with the loading icon NO LONGER APPEARS, but the backend is basically doing its job
- when making changes to fields in any of the detail pages, besides the Save button there should also be a Discard Changes button to reset any changes the user doesn't want.
- wherever there's gpt-generated values (such as customer detail page once CSF data extracted and input to customer values, or Upload files section when gpt labels the file type), include an info type msg not per field, but per form/box/section that says: "AI can make mistakes, make sure the data created is accurate."
  - http://localhost:3000/customers/[id]
- Change the Requirements pop up modal to update/keep reqs for credit cases to 3 buttons:
  - Cancel (nothing happens, changes reverted), Keep them as they are (keeps credit cases with prev version), Update these cases (updates the credit case reqs)
  - hitting the X on top SHOULD CANCEL THE OPERATION, NOT SAVE THE REQS
    - http://localhost:3000/requirements
- Requirements page seems to get cached to last user selections..if user is still on the page and cancels, then it's okay to leave the user's selection of files (maybe they want to select a few more or remove some), but if they move away from the page, the file selections should revert back to whatever is the active default requirements template for the org.
  - http://localhost:3000/requirements
- make all col lengths modifiable in dashboard view so user can set them to their taste. their latest modifications should be saved as their default, not going back to global defaults
  - http://localhost:3000/credit-cases

backend
