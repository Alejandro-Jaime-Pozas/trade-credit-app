frontend:
- when Documents in customer detail gets saturated, do a natural scroll instead of filling all the space up which looks terrible
  - http://localhost:3000/customers/1
- When modifying a credit case's documents required directly in the credit cases detail page, when user clicks done, the same modal prompt should appear to allow user to update all, cancel, or leave existing cc reqs
  - http://localhost:3000/credit-cases/1
- if user manually changes an uploaded doc's label (file type name), it should trigger the same functions as Required Documents being complete and whatever else happens IF that manual change completes all req docs..(status changing)
  - http://localhost:3000/credit-cases/1
- When pressing Cancel button in save required documents template, the https request is a patch, is that best practice for a cancel button, or just don't do anything? fix this for efficiency
  - http://localhost:3000/requirements

backend
