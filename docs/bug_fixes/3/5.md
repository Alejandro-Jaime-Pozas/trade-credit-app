frontend: DONE 2026-09-01
- DONE — when Documents in customer detail gets saturated, do a natural scroll instead of filling all the space up which looks terrible
  - http://localhost:3000/customers/1
  - `DocumentList` caps at 6 documents and scrolls past that. `FileTypeSelect` had to move its
    dropdown into a portal first — a scroll container clips its descendants, so the panel would
    have been sliced off on the rows a user scrolls down to correct.
- DONE — When modifying a credit case's documents required directly in the credit cases detail page, when user clicks done, the same modal prompt should appear to allow user to update all, cancel, or leave existing cc reqs
  - http://localhost:3000/credit-cases/1
  - Edits are now a draft until Done. Done raises the shared `ImpactWarning`: Update all (this
    case + the org default + the other open cases), Only this case, or Cancel (writes nothing).
- DONE — if user manually changes an uploaded doc's label (file type name), it should trigger the same functions as Required Documents being complete and whatever else happens IF that manual change completes all req docs..(status changing)
  - http://localhost:3000/credit-cases/1
  - `UploadDocumentViewSet.perform_update` now calls `handle_manual_requirement_change`. It had
    `create` and `perform_destroy` but no update, so this was the one path that never recomputed
    the case. Moving a document between cases recomputes both ends.
- DONE — When pressing Cancel button in save required documents template, the https request is a patch, is that best practice for a cancel button, or just don't do anything? fix this for efficiency
  - http://localhost:3000/requirements
  - Save now previews before writing. `POST /requirement-templates/{id}/preview-impact/` diffs a
    list that only exists in the request body, so Cancel sends nothing at all.

backend
