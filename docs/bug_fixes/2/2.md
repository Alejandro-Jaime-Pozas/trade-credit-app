frontend
- filter window is cut off on left side
  - http://localhost:3000/credit-cases
  - /Users/Alex/Documents/Coding/Applications/trade_credit_app/docs/bug_fixes/2/image.png
- buttons are a bit small, make them slightly bigger, hand should appear when hovered over vs plan pointer/cursor
  - http://localhost:3000/credit-cases
  - /Users/Alex/Documents/Coding/Applications/trade_credit_app/docs/bug_fixes/2/image copy.png
- customer column hyperlink should now link to that specific credit case, not the customer detail page anymore
  - http://localhost:3000/credit-cases
- verdict in credit case cannot be modified, must make it modifiable like status and assigned_to
  - http://localhost:3000/credit-cases/<id>
- when pressing save, user has no idea if it was successfully saved or not, fix
  - http://localhost:3000/credit-cases/<id>
- when deleting a credit case or customer in their respective page, pop up show delete failed but it was successful, fix. perhaps this bug is with all delete buttons across all UI pages, check and fix.
  - http://localhost:3000/credit-cases/<id>
  - FIX: must be an are you sure you want to delete for any delete button first, to securely delete the object
    - this goes for ANY DELETE OR REMOVE OR SIMILAR COMMAND IN THE ENTIRE APP
- customer detail page should include a list of that customer's credit cases (same cols as credit cases dashboard view)
- show a small circle to the right of the status column's value:
  - complete=green
  - missing docs=orange
  - pending ai verdict=loading icon
  - buro de credito rejected=red
  - pending final verdict=yellow
  - FIX: should be on the left, not right, and very close/little maring bw circle and status text value
- Upload files section in credit case detail page should be sorted by most recent file uploads
- on every page that allows file uploads, change the functionality so that when the user chooses the files and clicks submit in their os finder/explorer system, that triggers the file uploads, remove the Upload button please, not needed once implemented
- on credit case detail page, when all required docs are uploaded, user needs to refresh page to get updated status, can't you use react to refresh the status automatically without full page refresh?
- when files are uploading, should show animated 'loading' type icon or something
- since gpt can fail, allow user to update a specific document's file type name so that if gpt fails to label it correctly, the user can fix it. make it a dropdown search combo user can use.
- pressing esc on modals/popup windows should close them
- on requirements page, there is no cancel button in modal, fix
  - http://localhost:3000/requirements
-

backend
- min pwd length when creating acct is not currently enforcing a min
  - http://localhost:3000/signup password field
