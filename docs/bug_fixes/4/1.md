frontend: DONE 2026-09-01
- DONE — when a user custom field for credit case already has values, the user SHOULD NOT have to input text if values already exist, user should be able to search/dropdown like the create a customer field in new credit case page
  - `ValueCombobox` replaces the `<datalist>`: every recorded value is listed on click or
    ArrowDown, narrows as you type, and a brand new value can still just be typed.
- DONE — user should be able to drag and drop columns in credit case dashboard as they like, and always have their last config saved/persist for any future sessions
  - Drag a heading, or use Move up/down in the new Columns menu (dragging is mouse-only).
    Saved in localStorage per user, via `lib/layoutPrefs.ts`.
- DONE — user should be able to choose which columns they want visible vs which they do not, by default if they create a custom field, it should show up as a new column at the end of the credit case table, but user should be able to toggle hide/unhide the column
  - Checkboxes in the Columns menu. A new custom field lands at the end and visible even for a
    user who rearranged their columns long ago. The last visible column cannot be hidden.
- DONE — instead of Custom fields being its own section for user custom values, put them under Details section along with other fields that can be updated. Also, allow user to drag and drop and re-organize all fields + values under Details section... For user custom fields specifically, include a small blue icon user can hover over which says `custom field`.
  - One grid of identical `DetailField` cards, all draggable, order saved. Custom fields carry a
    blue "Custom field" icon and now save with the section's own Save button.
- DONE — for Verdict deadline (days) field: move the long description to a hover info icon, include a placeholder with the company's default deadline
  - The paragraph is an `InfoTip`; the placeholder reads e.g. `7 (organization default)`.
- DONE — in credit case detail page, at top of Details section, user should be able to `Add Field` button from there instead of manually going to the labels page.
  - Inline "+ Add field" at the top of Details. Validated client-side; not backfilled onto
    existing cases, and it says so.

backend: DONE 2026-09-01
- DONE — update Currency globally to now accept USD as well
  - `CurrencyName.USD` + migration `0006_alter_creditcase_currency`. The frontend list lives in
    `CURRENCY_OPTIONS` so the detail page and the new case wizard cannot drift apart.
