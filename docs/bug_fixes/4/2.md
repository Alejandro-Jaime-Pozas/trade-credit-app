frontend: DONE 2026-09-02
- DONE — when user types in a custom field in credit case detail, add option to allow user to create a new value for that custom field, so they don't have to manually go to Custom fields url to create new values
  - http://localhost:3000/credit-cases/19
  - Note on the premise: there is no page that creates custom field VALUES. `/labels` creates
    the fields; a value exists once a case is saved with it, and typing one already worked. The
    problem was that the dropdown of existing values read as the only permitted answers.
  - Typing text that matches nothing now shows `+ Create "Puebla"` at the foot of the list, with
    a footer saying it is added when the case is saved. Not offered when the value already
    exists (case-insensitive), so it cannot be used to create a duplicate spelling.
  - A field with no values at all now opens the same panel as soon as you type — it used to be a
    bare text box, and that is the field a user is most likely to be confused by.
  - Custom field boxes read "Pick or type a value" before anything is opened.
