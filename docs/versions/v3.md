# v3

## Scope

- The one thing I'd add
  - Log every override of a recommendation, with a name, timestamp, and required justification. Maybe 10 hours.
  - It's the cheapest feature in the product and the highest-leverage. Right now a branch can approve a marginal case informally and nobody reconstructs it later — the RDR case is exactly this, where Torreón approved what Monterrey Norte had rejected. The moment overrides are attributable, behavior changes before anyone reads the log.
  - It's also the feature that makes your bad-debt claim measurable at renewal, which is what lets you charge for Phase 2 on their data instead of my benchmarks.

## Moved out of v2

Moved on 2026-08-31 after sizing each item against what the codebase actually has today. These
are all still wanted - the reason each one is here is cost or a missing prerequisite, not doubt
about the value.

### Deadline email notifications

Emails to the users assigned to a credit case as it approaches or passes its verdict deadline,
plus per-user control over which cases or labels they hear about.

Deferred because the app has NO email infrastructure of any kind: no `EMAIL_BACKEND`, no SMTP
settings, no `send_mail` call anywhere, and no email package in `backend/requirements.txt`.
There is also no scheduler - Celery runs, but there is no Celery Beat service, so nothing can
run recurring work at all. Shipping this means standing up two new pieces of production
infrastructure, plus a notification-preferences model and enough sent-state to stop a case that
is three days overdue from emailing someone every hour.

The v2 deadline field is the prerequisite and lands first. Seeing the deadline on the dashboard
is most of the value; email is the last stretch of it at several times the cost.

BEFORE BUILDING THIS: `CreditCase.assigned_to` is a single ForeignKey, but the requirement is
worded as "users" plural. Multiple watchers needs a many-to-many, which is a migration - decide
the shape when this is picked up.

### AI-written verdict

A default AI verdict offered alongside the human decision, eventually growing into a chat over a
credit case.

Deferred because the summary (v2 item 4) should ship first and be trusted before the app starts
recommending decisions. Same legacy-code warning applies - see that item.

### User-editable AI summary

Letting a user rewrite the generated summary.

Deferred deliberately, not for cost - it is a text field and a form. Do not build the editor
before reading real generated summaries: if they are good, nobody edits them, and if they are
bad, the fix is the prompt rather than a text box. Cheap to add whenever the evidence says to.

### Server-side dashboard filtering

Not on the original list; it is the prerequisite the two items below both depend on.

The credit case dashboard currently filters and sorts entirely in the browser. Anything that
queries across more rows than a user's own dashboard page holds needs the filtering to move to
the backend first.

### Analytics-driven querying

Ad-hoc queries across cases and documents, e.g. "show me every pagare". Needs server-side
filtering above.

### Documents dashboard

Filter documents by credit case or by the organization's own labels, and view or filter the
extracted data - e.g. every pagare for three given sucursales, with its expiry date, which is
already extracted and only needs querying.

Much cheaper once v2 ships, since it is largely the union of pagare expiry extraction, labels
as filters, and a new page on top of server-side filtering.

### SSO

An auth overhaul that needs an identity provider and touches the organization model. Nothing is
blocked on it today.

### Excel and PDF export

CSV is a v2 stretch because the filtered rows are already in the browser. Excel and PDF need
real generation work and belong here.


## DB Models

## Backend

## Constraints

## Signup Workflow

## Credit Case Workflow

- backend analyzes docs, extracts data, extracts relevant required data, checks if file and data requirements satisfied (including dates required, accurate data, etc)


## Auth and Permissions

## Frontend UI Views

- create customer view
  - allow user to upload the constancia CSF to extract customer details first for faster extraction/less errors

### Nice-to-haves (not priority)

- auto-save on customer/credit case edit forms, instead of requiring explicit "Save" clicks
  - event-driven, not polling: debounce onChange (~800ms-1s of inactivity), then PATCH just the changed field(s)
  - optimistic UI with a "Saving.../Saved" indicator; retry with backoff on failure
  - also flush the save on blur and on navigation away (e.g. `beforeunload` / route change, via `navigator.sendBeacon` so it survives tab close)
  - periodic safety-net flush (e.g. every 30s) in case the user never stops typing
  - not needed: OT/CRDT-style collaborative conflict resolution (Google Docs/Figma-style) — this app isn't multi-user-per-record realtime, so last-write-wins per field is sufficient

---

## User-created file types

Deferred out of v1 on 2026-08-11. What SHIPPED in v1: an organization builds its own requirement
templates (strictly-required vs optional, set a default, add/remove per customer) — but only from the
app-provided catalog of file types. See `docs/architecture/decisions.md` > "Document requirements".

What is still MISSING is the ability to invent a file type that isn't in that catalog, e.g. typing
"Carta de poderes" when the app has never heard of it.

The v1 architecture was built so this is an addition, not a rewrite:

- `storage.FileType.organization` is already nullable. `NULL` = app-provided/global; set = that
  organization's own. Creating an org-owned type is a new ROW and can never modify a global one.
  `unique(organization, key)` plus a partial unique index on `key WHERE organization IS NULL` already
  keep both namespaces clean.
- `UploadDocument.file_type_name` is already a plain `CharField` with no `choices`, so an
  organization-specific key stores fine.
- The GPT classifier's allowed-values schema is already built per request rather than at import
  (`GPTService.get_file_type_name_json_schema`).
- Requirement templates already reference `FileType` by FK, so a custom type slots in with no
  special-casing.

Remaining work, roughly in order:

1. Make `FileTypeViewSet` writable for org-owned rows. Slugify `key` from the user's label; enforce
   immutability after create (documents are stored under it).
2. Add an `extraction_schema` JSONField to `FileType`. NOTE: `gpt.py` passes a JSON Schema to OpenAI
   structured outputs and only uses pydantic to GENERATE one — so a custom type can store JSON Schema
   directly. No runtime `create_model()` / code generation is needed. Resolution becomes:
   `spec.pydantic_model.model_json_schema()` for globals, the stored schema for custom ones.
3. Thread the organization through `get_file_type_name_json_schema()` so classification offers
   global + that org's keys.
4. Decide the shadowing rule: what happens when an org owns `carta_de_poder` and the app later ships
   a global type with the same key (org wins / merge / rename-on-conflict). Deliberately left open.
5. Frontend: `GET /file-types/` returns globals and the org's own in one list, custom ones badged; the
   template editor gets an "+ Add custom requirement" flow (name + the fields to extract).

Degrades gracefully in the meantime: the friendly-file-name `if/elif` chain in
`storage/services/db_object_handling.py` won't match a custom type, so `friendly_file_name` just stays
null rather than erroring.

## Candidate file types for the catalog

**See `docs/architecture/file_type_catalog_reference.md`** — the full set of document types relevant
to Mexican trade credit, with a Status column marking which shipped in v1 and which were deliberately
deferred here. That file also records why each deferred row exists, so nothing needs re-deciding when
it is picked up.

Adding a deferred type is: move its row into `core/file_type_catalog.py` as one `FileTypeSpec`
(plus a pydantic extraction model, unless it is presence-only), then `manage.py sync_file_types`.

Deferred to v2, grouped as in that file:

	- Financial: estados financieros dictaminados / internos, carátula de estado de cuenta,
	  antigüedad de saldos CxC y CxP, relación de deuda, proyecciones financieras,
	  relación de activos fijos, estado financiero personal, cédula profesional del contador
	- Tax: alta en Hacienda (R1), cédula de identificación fiscal
	- Legal: folio mercantil (RPPC), comprobante de domicilio comercial, declaración de beneficiario controlador
	- Credit: solicitud de crédito, autorización de consulta a Buró, reporte de Buró,
	  referencias bancarias, aviso de privacidad, contrato de crédito
	- Collateral: escritura del inmueble, certificado de libertad de gravamen, avalúo,
	  póliza de seguro, inscripción en el RUG
	- Operational: orden de compra / contrato de suministro

Also carried forward from that file, and NOT solved by adding catalog rows:

	- per-person requirements (one ID per aval / beneficial owner) — a `quantity` field on
	  `CreditCaseRequirement`, since completion currently compares SETS of file type keys
	- enforcing `months_required` — `CreditCase.missing_file_type_names` ignores it today
	- equivalence groups — dropped from v1 because both `registro_fiscal` alternatives
	  (R1, cédula de identificación fiscal) are deferred, leaving a one-member group.
	  Revisit when either lands.
	- the Buró consent gap — `carta_autorizacion_buro` is deferred while
	  `BURO_DE_CREDITO_REJECTED` and the integration already exist
	- a display field for UI grouping — `category` cannot carry it, it means recency
