# v1

## Scope

- user should be able to:
  - [x] create acct
  - [x] log in
  - [x] log out (frontend clears local tokens; backend refresh-token blacklist route not wired into urls)
  - [x] add/update/delete customer detail
  - [x] add/update/delete customer contact detail
  - [x] add/update/delete credit case detail
  - [x] upload files to customer or credit case
  - [ ] view dashboard of credit cases, filter/order by status, days since creation (status filter is client-side only; no "days since creation" sort yet)
- [x] file type name extraction gpt
- [x] basic data extraction (IF ALREADY HAVE THIS SETUP PREVIOUSLY)
  - [x] specify exact fields by file type name, such as RFC for CSF
  - [x] specify for which files (maybe just for CSF for now)
- [ ] solid UI users can actually use (subjective, pending user validation)
- [x] default file type name requirements, user can't modify for now


## DB Models

- [x] User
- [x] Organization - new name
- [x] Customer - new
- [x] CustomerContact - new
- [x] CreditCase - new
- [x] UploadDocument - modified
- [x] DocumentDataExtract - new (model exists but is never instantiated; extraction currently writes to `UploadDocument.extracted_data` instead)
- [x] Label - new


## Constraints

- no RBAC yet, any new user is admin by default
- no credit evaluation analysis yet
- business customers (personas morales) only
- no user-defined requirements, only globally defined ones (Acta, CSF, etc)


## Implementation

### Signup Workflow

- [x] user: types email, pwd
- [x] backend: new user created
- [x] user: clicks log in, types credentials
- [x] backend: logs user in
- [x] backend: sets the organization to the user's email for now (implement later or allow user to change org name)


### Credit Case Workflow

- [x] user clicks on create new credit case
- [x] user creates new customer profile to link to new credit case (or links to existing customer)
  - Customer model fields in form
    - filled out manually by user in v1
      - can (later) be extracted from docs like constancia CSF
    - key fields
      - rfc
      - legal name
      - name
      - address (multiple address fields in Customer model)
- [ ] backend auto-create credit case linked to the customer profile (not implemented; user must manually create the credit case after creating a customer)
- [x] credit case default required files specified in @backend/core/constants.py as CREDIT_CASE_FILE_TYPE_NAMES_REQUIRED
  - [x] user can input/update fields for created credit case
    - requested_amount
    - currency
    - requested_term_days
    - assigned_to
- [x] user uploads files (UploadDocument model) to credit case detail view (can bulk load, or single load)
  - this uploads the file to UploadDocument @backend/storage/models.py, linking the file to customer and credit case
    - file has no link yet to a specific file type name required
- [x] backend extracts the file contents to match the file type with a required file type if exists, else marks as unknown file type (this requires the openai/chatgpt integration in @backend/integrations/openai/services/gpt.py to extract the correct file type name)
- [x] user can then change credit case status as it progresses (missing data, pending credit review, pending authorization), upload more files, change credit case model data (status choices implemented, though current values differ from the names listed here: `missing_documents`, `pending_ai_verdict`, `buro_de_credito_rejected`, `pending_final_verdict`, `complete`)


### Auth and Permissions

- [x] user default is admin for now, no RBAC for now (implement later)
- [x] user creates account
  - plain email (implement Google auth, Microsoft auth later)
- [x] user logs in
- [ ] user logs out (same gap as Scope section: no server-side token invalidation)


### Frontend UI Views

- [x] dashboard view
  - [x] includes rows of credit cases, status, start date among other important columns
  - [ ] users can filter view by status, date, etc (status filter only, client-side; no date-based sort/filter)
- [x] create customer view
  - [x] let user fill out fields
  - fields: rfc, nombre comercial, address, etc
- [x] customer detail view (customer already created)
  - [x] user can update customer profile fields
  - fields: rfc, nombre comercial, address, etc
- [ ] create credit case view (auto-created when new customer is created) (view is implemented, but the auto-create-on-customer-creation behavior is not)
  - [x] user can add/update credit case fields
  - [x] user adds/updates internal customer credit request fields: amount, term (net 30/45/60, etc)
  - [x] user can upload new customer files
    - [x] then backend extracts the file type, useful data for credit evaluation
    - files: CSF, Acta Const, Bank Stmts, Financial Stmts, Poderes Notariados, ID oficial, comp domicilio
  - [ ] user can modify strict requirements to consider solicitud as complete (not implemented; also conflicts with Constraints: "no user-defined requirements")
- [x] credit case detail view (credit case already created)
  - [ ] user can update fields, files, requirements for completion (fields/files editing implemented; requirements-for-completion editing not implemented)
    - files: CSF, Acta Const, Bank Stmts, Financial Stmts, Poderes Notariados, ID oficial, comp domicilio
