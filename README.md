# Launching The App For Dev

- terminal 1 - launch all docker services:
  - `docker compose up`
- terminal 2 - watch for api changes to auto-update types for frontend:
  - `cd frontend && npm run watch-api`
