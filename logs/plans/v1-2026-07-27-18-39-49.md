# Plan

1. Audit the codebase (backend models/views/serializers, frontend views, GPT integration) against every actionable item in `docs/versions/v1.md` using an Explore subagent, since this required more than 3 lookups across backend and frontend.
2. Convert actionable list items (Scope, DB Models, Signup Workflow, Credit Case Workflow steps, Auth and Permissions, Frontend UI Views) into GitHub-flavored markdown checklists (`- [ ]` / `- [x]`).
3. Leave purely descriptive/spec sub-bullets (field lists like "fields: rfc, nombre comercial...") and the `Constraints` section as plain bullets, since they describe data shape or rules rather than completable tasks.
4. Check off (`[x]`) items confirmed implemented by the audit; leave unchecked (`[ ]`) items that are missing or only partially implemented, adding a short inline note explaining the gap.
