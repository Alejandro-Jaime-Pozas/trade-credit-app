# Prompt — bug fixes 4/1

Logged 2026-09-01 19:52:57. Source: `docs/bug_fixes/4/1.md` (plus its two screenshots).

## User prompt (verbatim)

> implement '/Users/Alex/Documents/Coding/Applications/trade_credit_app/docs/bug_fixes/4/1.md'
> no questions, go.

"No questions" means every ambiguity below was resolved by judgement and stated in the
implementation log rather than asked about.

## The seven items

Frontend:

1. A custom field that already has values should offer them as a searchable dropdown rather
   than making the user retype text — like the customer field on the new credit case page.
2. Drag and drop to reorder columns on the credit cases dashboard, persisted across sessions.
3. Choose which columns are visible; a new custom field appears as a new column at the end and
   can be hidden/unhidden.
4. Fold Custom fields into the Details section on the credit case detail page. Every field+value
   pair looks the same whether or not it can be edited, all of them can be dragged into whatever
   order the user likes, and custom fields carry a small blue icon reading "custom field".
5. Verdict deadline (days): move the long description to a hover info icon, and put the
   organization's own default in the placeholder.
6. An "Add Field" button at the top of Details, so the user does not have to go to `/labels`.

Backend:

7. Accept USD as a currency, globally.
